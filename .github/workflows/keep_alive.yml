name: Keep Streamlit App Awake

on:
  schedule:
    - cron: '0 6 */3 * *'
  workflow_dispatch:

# Add these lines to give the bot write access to push changes
permissions:
  contents: write

jobs:
  keep-awake:
    runs-on: ubuntu-latest
    steps:
...
